# Set up

`pipenv install`

# Run

`make run` or `make run-dev`

# Check

`make check`

# Format

`make reformat`

# Test

`make test`

# Deploy

Make sure you have CDK installed globally (else `npm install -g aws-cdk`)

`make deploy`
